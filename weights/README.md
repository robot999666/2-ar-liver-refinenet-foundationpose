# weights

- `depth_anything_v2_vitl.pth`：Depth Anything V2 Large 官方权重，Git 忽略，必须外部备份。
- `refinenet_patient1_02_best.pth`：固定 RefineNet 权重，已纳入 Git；当前帧没有新训练权重时作为默认回退。

路径、字节数和 SHA256 见根目录 `MANIFEST.md`。
