# Runtime dependencies for scripts/06_train_refine_net.py on:
# pytorch / pytorch-2.5.1 / cuda12.4 / python3.12
#
# The image already provides CUDA-enabled PyTorch, so torch/torchvision/
# torchaudio are intentionally omitted to avoid replacing the preinstalled
# CUDA build with pip wheels.

numpy==1.26.4
scipy==1.15.3
matplotlib==3.10.3
tqdm==4.67.1
open3d==0.19.0
opencv-contrib-python-headless==4.11.0.86
