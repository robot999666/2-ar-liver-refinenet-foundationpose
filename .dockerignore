# Dockerfile installs the runtime only. Project code, source data, and weights
# are mounted at /workspace/AR when the container starts, so do not send them
# into the image build context.
*
!Dockerfile
!requirements.txt
